# Homework

## Write a program in c++ to

1. Split a string by separator where both are provided by user.
2. Reverse an array
3. Check if all strings in an array are palindrome.
4. Check if all strings in an array joined form palindrome.
5. Check if all strings in an array are anagrams.
